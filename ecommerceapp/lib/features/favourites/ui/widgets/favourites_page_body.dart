import 'package:flutter/material.dart';

class FavouritesPageBody extends StatelessWidget {
  const FavouritesPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        
      ],
    );
  }
}